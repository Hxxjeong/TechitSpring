spring:
  application:
    name: springjdbc

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exampledb
    username: ${username}
    password: ${password}